description: An empty template for testing
pages:
  - type: top
    path: /
    sort: timestamp_utc desc
    blockSelector: .aggregation
    count: 30

  - path: /archive/
    type: all
    blockSelector: .aggregation
    paginate: 20
